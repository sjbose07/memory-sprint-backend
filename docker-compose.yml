version: '3.8'

services:
  backend:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    restart: unless-stopped
